const loader = (id, spId) => {
    let btn = document.getElementById(id);
    // let spBtn = document.getElementById(idId);
    btn.innerHTML = "Loading...";
    // btn.innerHTML = "";
    // spBtn.style.display = "block";
} 